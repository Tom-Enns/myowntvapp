"""Stream resolvers — extract m3u8 URLs from embed/iframe pages.

Each resolver declares what URL domains it handles. The registry
auto-detects the right resolver based on the embed URL's hostname.
"""
